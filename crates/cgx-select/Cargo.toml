[package]
name = "cgx-select"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "cgx language-agnostic node selector engine: per-language tokenizers over a uniform ::-segmented form, optimistic multi-parse, a Thompson-NFA segment matcher (glob/alternation/affix-negation, no regex, no expansion), and a regime-(b) scan predicate with honest provenance/truncation diagnostics."

[dependencies]
cgx-core.workspace = true
serde.workspace = true
thiserror.workspace = true
inventory = "0.3"
