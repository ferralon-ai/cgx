[package]
name = "cgx-query"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "cgx Layer-1 query engine: a GraphView over a linked graph plus the callers/callees/reachability/paths subcommands as direct, deterministic graph algorithms with per-walk path-relative transience (GM-4)."

[dependencies]
cgx-core.workspace = true

[dev-dependencies]
cgx-store = { path = "../cgx-store" }
